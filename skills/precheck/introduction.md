`/precheck` is the mandatory pre-commit gate -- it verifies your branch and issue compliance, runs the code reviewer, and presents a checklist before any commit happens. Use it when your implementation work is done and you are ready to commit. After the checklist appears, wait for approval before proceeding. This is the single most important workflow skill -- it prevents unreviewed code from being pushed. For the full commit workflow, run `/ccwork tour workflow`.
