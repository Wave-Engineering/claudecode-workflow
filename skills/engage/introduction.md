`/engage` is the post-compaction (or session-start) ritual that reloads CLAUDE.md, confirms the mandatory rules, and restores your working context. Use it after context compaction or at the start of a new session to get back up to speed. Your durable memories and any auto-crystallized state are loaded automatically — `/engage` is the thaw. For a walkthrough of the session lifecycle, run `/ccwork tour foundations`.
