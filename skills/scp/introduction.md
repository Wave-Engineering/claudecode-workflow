`/scp` handles the stage-commit-push workflow with full context awareness -- it stages changes, creates a well-formatted commit message, pushes, and opens a PR/MR if one does not exist. Use it after `/precheck` has been approved. It enforces branch safety (refuses to commit on protected branches) and links issues automatically from the branch name. For the full commit workflow walkthrough, run `/ccwork tour workflow`.
