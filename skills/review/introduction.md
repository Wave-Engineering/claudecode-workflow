`/review` runs a targeted code review using the code-reviewer subagent on staged changes, a branch diff, or a specific file. Use it when you want a review outside the normal `/precheck` flow -- for example, reviewing a single file mid-implementation or checking a colleague's branch. Pass `staged`, `branch`, or a file path to control scope. For more on the review process, run `/ccwork tour workflow`.
