`/assesswaves` quickly evaluates whether a set of work items can benefit from parallel agent execution via the wave pattern. Use it before `/prepwaves` when you have an epic or a batch of issues and want to know if splitting them across agents is worth the overhead. It analyzes dependency graphs and file-level conflicts without creating any issues or plans. For a hands-on walkthrough of the wave skills, run `/ccwork tour`.
