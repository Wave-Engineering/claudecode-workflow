`/view` opens a file or URL in a GUI application for read-only viewing, launching it asynchronously so the chat continues. Use it when you want to eyeball a file, image, PDF, or webpage without modifying it. Unlike `/edit` (modification intent), `/view` prefers lightweight viewers. Preferred applications per file type are configured in your user preferences. For more on the view/edit pair, run `/ccwork tour`.
