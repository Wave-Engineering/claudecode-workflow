`/cryo` preserves your session state before context compaction by freezing the plan file and task list into durable storage. Use it when Claude warns that compaction is imminent or when you want to checkpoint a long session. The complementary skill `/engage` thaws the frozen state in a new or compacted session. For a walkthrough of the session lifecycle, run `/ccwork tour foundations`.
